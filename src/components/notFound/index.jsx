import React from 'react';
import PropTypes from 'prop-types';

const NotFound = (props) => {
    return (
        <div>
            <h1>NOT FOUND</h1>
        </div>
    );
};

NotFound.propTypes = {};

export default NotFound;
